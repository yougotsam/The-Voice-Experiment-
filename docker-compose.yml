services:
  voice-agent:
    build: .
    ports:
      - "8000:8000"
      - "3000:3000"
    env_file:
      - .env
    environment:
      - CORS_ORIGINS=http://localhost:3000
    restart: unless-stopped
